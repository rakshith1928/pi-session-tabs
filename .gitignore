node_modules/
*.log

# Pi project-local runtime (transient, not shareable). Keep .pi/prompts,
# .pi/settings.json and .pi/extensions trackable for contributors.
.pi/subagents/
.pi/hf-sessions/
.pi/hf-sessions-backup/
.pi_config/
tui-debug.log

# Maintainer-only release prompt — contributors must not publish, so keep it
# local and out of clones (Pi still loads it from disk for the maintainer).
.pi/prompts/release.md

# Deep internal design spec — kept local, not published to the repo.
DESIGN.md

# Planning artifacts (SDD plans) — kept local, not published to the repo.
docs/superpowers/
